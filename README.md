# photographer

## Lightroom downloads
Each client gallery has a **Lightroom download link** field. Paste the Lightroom web gallery URL when creating or editing a gallery. The client-facing **Download full-resolution photos** button opens that Lightroom gallery in a new tab. The Django site does not generate or store a ZIP for client downloads.

After updating, run:
`python manage.py migrate`
